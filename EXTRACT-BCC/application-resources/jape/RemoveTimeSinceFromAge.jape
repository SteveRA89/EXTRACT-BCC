Phase: RemoveTimeSinceFromAge
Input:  Age TimeSince
Options: control=all

// not in pipeline so delete me
Rule: RemoveTimeSinceFromAge
(   
    ({TimeSince within Age}):umls
):match
-->
:umls { 
  inputAS.removeAll(umlsAnnots);
}

