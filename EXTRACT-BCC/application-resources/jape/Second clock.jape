Phase: SecondClockFace
Input: Numeric Token Split ClockFace
Options: control=appelt

Rule: SecondClockFace
(
	({Numeric.value < 13, Numeric.value > 0})
    ({Token.string == "+"}|{Token.string == "&"}|{Token.string == "and"})
    ({ClockFace})

):match
-->
:match.ClockFace = { rule = SecondClockFace, time = :match.Numeric.value}