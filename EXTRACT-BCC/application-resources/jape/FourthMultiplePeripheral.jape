Phase: MultiplePeripheral
Input: Distance_to Dimension Measurements ClockFace NewLine Sepatator
Options: control=appelt

Rule: FourthMultiplePeripheral
Priority:100
(
    ({Measurements.MeasurementType == DeepClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    (({Distance_to})
    ({ClockFace})
    ({Dimension})):match

):match1
-->
:match.Measurements = { rule = FourthMultiplePeripheral, MeasurementType = "PeripheralClearance", 
MeasurementValue = :match.Dimension.N1, 
MeasurementUnit = :match.Dimension.units,
PeripheralClockPosition = :match.ClockFace.time}

Rule: FourthMultiplePeripheral2
Priority:100
(
    (({Distance_to})
    ({ClockFace})
    ({Dimension})):match
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == PeripheralClearance})
    ({Sepatator})?
    ({NewLine})?
    ({Sepatator})?
    ({NewLine})?
    ({Measurements.MeasurementType == DeepClearance})

):match1
-->
:match.Measurements = { rule = FourthMultiplePeripheral2, MeasurementType = "PeripheralClearance", 
MeasurementValue = :match.Dimension.N1, 
MeasurementUnit = :match.Dimension.units,
PeripheralClockPosition = :match.ClockFace.time}