Phase: Sepatator
Input: Token
Options: control=appelt


Rule: TagLetters1
Priority: 100
//A) or B) not a) or b)
(
	({Token.kind == punctuation, Token.length == 1, Token.string == ","}|{Token.kind == punctuation, Token.length == 1, Token.string == ";"}):Tag
):match
-->
:match.Sepatator = { rule = Sepatator1, value = :match@string, string = :Tag.Token.string}